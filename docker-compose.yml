version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3113:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
      - BASE_PATH=/partner2slr
      - NEXT_PUBLIC_BASE_PATH=/partner2slr
